rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Christmas items collection - allow all operations for family app
    match /christmas-items/{itemId} {
      allow read, write: if true;
    }
  }
}
